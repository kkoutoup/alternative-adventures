<div class="container">
  <div class="row">
    <div class="col-12 col-lg-10 offset-lg-1">
      <%= link_to 'All Alternative Adventures', experiences_path, class:"alt-adventures-link" %>
      <h1 class="mt-3"><%= @experience.title %></h1>
      <%= image_tag @experience.photo, class:"img-fluid main" %>
    </div>
  </div><!--row-->
  <div class="row mt-3">
    <div class="col-5 offset-lg-1">
      <p class="grey-text"><i class="fas fa-user"></i> Offered by <%= @experience.user.first_name %> <%= @experience.user.last_name %></p>
      <p class="grey-text"><i class="fas fa-basketball-ball"></i> <%= @experience.experience_type %> adventure</p>
    </div>
    <div class="col-5">
      <p class="grey-text"><i class="fas fa-book-reader"></i> <%= @experience.description %></p>
      <p class="grey-text"><i class="fas fa-coins"></i> £ <%= @experience.price %></p>
    </div>
    <div class="col-5 offset-lg-1">
      <%= link_to "Book Experience", new_experience_booking_path(@experience), class:"btn btn-primary" %>
    </div>
  </div><!--row-->
</div><!--container-->
  <%# <%= link_to "See My Bookings", experience_bookings_path(@experience) %>
  <%# <hr> %>
  <%# <%= link_to "Edit Experience", edit_experience_path(@experience) %>
  <%# <hr> %>
  <%# <%= link_to "Delete Experience", experience_path(@experience), method: :delete, data: {confirm:"Are you sure you want to delete your offered experience?"} %>